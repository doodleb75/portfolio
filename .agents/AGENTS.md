# Project Rules

- Perform tasks using local Ollama (via `ollama-local` MCP server) to minimize external token usage where possible.
- Always append `[ollama work]` at the end of the response or work summary.
- Always respond in Korean (한글로 대답).

